<Window x:Class="FrontTerreno.ListaPersona"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontTerreno"
        mc:Ignorable="d"
        Title="ListaPersona" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="tablaPersona"  AutoGenerateColumns="False" CanUserAddRows="False" Height="380" Margin="32,0,32,0" VerticalAlignment="Center" Width="737">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Apellido Paterno" Binding="{Binding ApellidoPaterno}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Apellido Materno" Binding="{Binding ApellidoMaterno}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Direccion" Binding="{Binding Direccion}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Telefono" Binding="{Binding Telefono}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
